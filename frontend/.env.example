# For running the frontend outside Docker (npm run dev) against a backend
# started separately (dotnet run, or `docker compose up backend postgres`).
# Copy this file to .env.local and adjust if needed.
API_URL=http://localhost:5080
